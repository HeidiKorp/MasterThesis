import pandas as pd
import numpy as np
from transformTracks import getCenterPoint

# Create a dummy dataset with the first position of a vehicle in every exit
# Randomly select one track to match with it, predict its destination and based on the vehicle's current (5 datapoints) location and destination

# Check if the vehicle has already passed
# Calculate if the sv is below of above the line between tv ang middle point
# Check if the destination is after my exit

def predictAction(tv_origin, sv_x, sv_y, dest):





def main():
    print()

if "__name__" == __main__:
    main()